<h1>Hello Index</h1>
<%= link_to 'Home', :welcome_index %> 
<%= link_to 'New', new_card_reader_path %>
<h1></h1> 
<table>
  <tr>
    <th>Address</th>
    <th>Name</th>
  </tr>
 
  <% @card_readers.each do |card_reader| %>
    <tr>
      <td><%= card_reader.address %></td>
      <td><%= card_reader.name %></td>
      <td><%= link_to 'Show', card_reader_path(card_reader) %></td>
    </tr>
  <% end %>
</table>

<h1></h1>
<%= link_to 'Home', :welcome_index %> 
<%= link_to 'New', new_card_reader_path %>