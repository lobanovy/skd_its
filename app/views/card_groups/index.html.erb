<h1>Card Group</h1>
<%= link_to 'Home', :welcome_index %>
<%= link_to 'New Group', new_card_group_path %>

<h1></h1> 
<table>
  <tr>
    <th>Reader</th>
    <th>Group</th>
    <th></th>
  </tr>
 
  <% @card_groups.each do |card_group| %>
    <tr>
      <td><%= card_group.card_reader.name %></td>
      <td><%= card_group.card_reader_group.name %></td>
      <td><%= link_to 'Show', card_group_path(card_group) %></td>
    </tr>
  <% end %>
</table>

<h1></h1>
<%= link_to 'Home', :welcome_index %>
<%= link_to 'New Group', new_card_group_path %>