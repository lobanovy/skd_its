<h1>New Card_Reader_Group</h1>
<%= form_for @card_reader_group do |f| %>
<% if @card_reader_group.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@card_reader_group.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @card_reader_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :Group_name %><br>
    <%= f.text_field :name, size: "34x12" %>
  </p>
 
  <p>
    <%= f.submit "Save" %>
  </p>
<% end %>

<%= link_to 'Back', card_reader_groups_path %>