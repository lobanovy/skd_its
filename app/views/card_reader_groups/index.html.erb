<h1>Card_Reader_Groups</h1>
<%= link_to 'Home', :welcome_index %>
<%= link_to 'New Group', new_card_reader_group_path %>

<h1></h1> 
<table>
  <tr>
    <th>Group</th>
    <th></th>
    <th></th>
  </tr>
 
  <% @card_reader_groups.each do |card_reader_group| %>
    <tr>
      <td><%= card_reader_group.name %></td>
      <td><%= link_to 'Show', card_reader_group_path(card_reader_group) %></td>
    </tr>
  <% end %>
</table>

<h1></h1>
<%= link_to 'Home', :welcome_index %>
<%= link_to 'New Group', new_card_reader_group_path %>

